import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container:{
        
    },
    confirmButton:{
        backgroundColor: '#000',
        padding: 10,
        margin: 10,
        alignItems: 'center'
    },
    confirmText:{
        fontSize: 18,
        color: '#fff',
        fontWeight: 'bold'
    }
})

export default styles